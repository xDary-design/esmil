{
  "name": "monitor-pagina",
  "version": "1.0.0",
  "private": true,
  "description": "Robot que revisa una pagina web hasta que funcione, ingresa con un usuario y avisa por correo.",
  "scripts": {
    "install-browser": "playwright install --with-deps chromium",
    "check": "node check.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "nodemailer": "^10.0.1",
    "playwright": "^1.56.1"
  }
}
